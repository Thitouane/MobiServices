import 'package:flutter/material.dart';

class AtHomeDetailPage extends StatelessWidget {
  final commercant;
  const AtHomeDetailPage({
    Key? key,
    this.commercant
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
